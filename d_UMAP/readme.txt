umap read me

2022-10-24

run BRCA on the outlier clipping data
decoded dir, raw dir:
    hold
    hold

next: run with RobustScaling, sigmoid function

to solve:
    relative path breaking when archiving notebooks in shared dir (?)

